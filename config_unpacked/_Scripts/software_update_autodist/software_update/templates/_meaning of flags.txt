! - скрипт будет запускаться каждый раз, но последним

Остальные скрипты выполняются в порядке, в котором выдаёт dir /O

0 - для запуска скрипта в первую очередь. Явно указан только в software_update.cmd, для них (как и !) не будут создаваться журналы.
_ - будет запускаться после 0, но до букв. Предпочтительно помечать таким символом скрипты настройки.

